<!-- ./ Main -->
<div class="container">
  <div class="grid">
    <h1>Dashboard</h1>
   <a href="/dashboard/new"> <button>new +</button></a>
    <div>
      {{#each posts}}
      <h3>{{title}}</h3>
      <p>Created by: {{user.username}} | Date: {{format_date createdAt}}</p>
      <p>{{content}}</p>
        {{/each}}
    </div>
  </div>
</div>